import type { ThemeConfig } from 'antd'

/** 사용자(gen) 영역 AntD 테마. 관리자와 다른 스타일(색상/모서리 등)을 여기서 관리. */
export const genTheme: ThemeConfig = {
  token: {
    colorPrimary: '#00897b',
    borderRadius: 8,
  },
}
